// Module-level mouse ref — written by Hero.jsx event handler, read by HeroScene useFrame
// Kept separate so HeroScene can be lazy-loaded without a static import side-effect
export const heroMouse = { x: 0, y: 0 }
